import { observer } from "@core-ui/react-mobx-state"
import { Flex } from "@core-ui/react-mui-core"

export interface IJobsSliderProps {
  data: any[];
}

export const JobsSlider = observer(({ }: IJobsSliderProps) => {

  return <Flex fullWidth>

  </Flex>
})